#include <stdio.h>
int main ();
void printBinary(int num) {
    int a = 8;
    for (int i = a - 1; i >= 0; i--)
    {
        int bit = (num >> i) & 1;
        printf("%d", bit);
    }
}
int main() {
    int num;
    scanf("%d", &num);
    printBinary(num);
    printf("\n");
    return 0;
}
